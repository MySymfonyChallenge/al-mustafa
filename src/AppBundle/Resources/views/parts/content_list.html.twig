{% set extraParams = extraParams is defined ? extraParams %}

{% if items|length %}
    {% for hit in items %}
        {{ render_esi(controller('ez_content:viewAction', {
            'locationId': hit.mainLocationId,
            'viewType': viewType,
            'params': {
                'extra': extraParams
            }
        })) }}
    {% endfor %}
{% endif %}